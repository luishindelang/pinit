# Pinit — Historie 2026-09-07

Ausgelagert aus `PLAN.md`, weil die lebende Plandatei per `@import` in **jeder** Session
komplett geladen wird und die Root-`CLAUDE.md` verlangt, sie schlank zu halten. Hier steht
die abgeschlossene Historie des Tages: das Bau-Protokoll von Fassung 1.1, die Nachträge der
Plan-Brille und die vollständige Befundliste des ersten Code-Panels (Fassung 1.0).
**Nichts davon ist noch offen** — alle Kästchen sind abgehakt. Der aktuelle Stand steht in
`PLAN.md`.

**Zu den Verweisen in dieser Datei:** „→ E1/E2/E3“ meint die drei Entscheidungen der
Planungsrunde, die in `PLAN.md` im Abschnitt „Entschieden 2026-09-07“ stehen — nicht hier.
Alles andere (R…, O…, G…, N…, P…) ist innerhalb dieser Datei definiert.

*(Befund der `klarheit`-Brille im Code-Panel zu Fassung 1.1: `PLAN.md` war zu über der
Hälfte Archiv.)*

## Umgesetzt in Fassung 1.1 (2026-09-07, `/projekt-umsetzen`)
Gebaut in der geplanten Reihenfolge **R3 → R4 → R2/N1 → E1**, dazu die mechanischen 🟠.
Alles in `code/pinit.html`; `code/reissbrett-vorlage.html` per `vorlage-erzeugen.js`
nachgezogen; beide Artifacts neu veröffentlicht (mit `url`, weil die Datei umgezogen ist).

**Erledigt:**
- **R3** `beginEdit`: der frühe `return` im Zeitgeber setzt jetzt `editing = null` —
  Spiegelbild derselben Zeile in `renameSheet`. Der stumme Sync-Stillstand ist damit weg.
- **R4 / E2** `pending`/`pull()` sind ersetzt durch `wartend` (ein aufbewahrtes
  Snapshot-Objekt je Sammlung) plus **einen** Abarbeitungs-Pfad `abarbeiten()`, gerufen am
  Ende von `finish()`, im `mouseup` und am Ende von `done()` in `renameSheet`.
- **R2 / N2** `applySheets` hat den Wächter — und zwar `warteGrund()` = 
  `editing || drag || tabFeldAktiv()`, **nicht** nur `editing || drag`. Der Befund N2 hätte
  sonst genau das Umbenennen ungeschützt gelassen.
- **N1** Der Rücksetz-Zweig in `applySheets` prüft nicht mehr `sheets.size &&`, sondern
  „ist der offene Reiter gültig" (echter Reiter, impliziter `haupt`, oder der Auffang-Reiter
  mit Inhalt). Das leere Brett bei null Reitern ist damit weg.
- **E1** `deleteSheet` löscht **nur** `sheets/<id>`. Waisen-Regel als `wirkReiter()`;
  `refilter` und `sheetList` benutzen sie; der virtuelle Auffang-Reiter `_unsortiert` wird
  erzeugt, wenn Waisen daliegen, trägt die Anzahl, hat kein Löschkreuz, reagiert nicht auf
  Doppelklick, und ein hereinkommendes Dokument mit dieser Kennung wird verworfen.
  `putNode`/`putEdge` schreiben unverändert das **eigene** `sheet` — daran hängt die
  Umkehrbarkeit.
- **N4** Die Übergabe-Nutzlast trägt jetzt die **Feldnamen der Datenbank**
  (`kind`/`color`/`z`/`sheet`, Struktur `nodes`/`edges`/`sheets`/`meta.board`). Es gibt kein
  zweites Format mehr, das driften kann — die Fehlerklasse ist weg, nicht bewacht. Waisen
  werden beim Übergeben auf den ersten Reiter aufgelöst, damit beim Empfänger kein Verweis
  ins Leere zeigt.
- **O5** Die Fehler-Rückrufe für `sheets` und `meta/board` melden jetzt
  `Speicher-Fehler bei den Reitern: <code>` bzw. `… beim Titel: <code>`.
- **G6** Text aus der Datenbank wird beim Einlesen gekappt: Element 2000, Pfeil 200,
  Reitername 80 Zeichen.
- **N10-N16** in der Doku nachgezogen (Zahl gestrichen, Sortierung benannt, Landkarte
  vollständig, Bedienung beschrieben, Begriffe aufgelöst, siebter Testweg, G7-Streit).
- **Fassung** auf **1.1** gezählt, sichtbar in der Kopfzeile.

**Gemessen, nicht behauptet:**
- **Waisen-Regel am laufenden Brett:** Reiter angelegt, Element darauf, Reiter gelöscht →
  Reiterleiste zeigt `Unsortiert (1)`, Elementzahl **unverändert** (nichts gelöscht), das
  Element erscheint auf dem Auffang-Reiter, dort kein Löschkreuz.
- **Umkehrbarkeit:** ein Wegwerf-Element wurde einem Reiter zugeordnet, der Reiter-Eintrag
  gelöscht und das Element danach aus der Datenbank gelesen — es trägt **weiter**
  `sheet: "probereiter"`. Die Zuordnung überlebt das Löschen, ein Reiter mit derselben
  Kennung holt sie zurück. *(Probe danach entfernt.)*
- `vorlage-erzeugen.js` meldet „1 Zeile anders“; beide Dateien bestehen `node --check`.
- **Rot-Gegenprobe der beiden Kommandos** (im Bau-Protokoll ursprünglich nicht vermerkt,
  nachgetragen 2026-09-07 nach einem Panel-Befund): `node --check` über ein Skript mit
  zerstörter Funktionssignatur wurde **rot**, `vorlage-erzeugen.js` mit entferntem
  Titel-Anker **brach ab**. Sie lief also — nur die Notiz fehlte.

**NICHT geprüft — gehört dem Menschen (siebter Testweg):**
R2, R3 und R4 sind Nebenläufigkeits-Fixes. Ihr Verhalten braucht **zwei gleichzeitige
Betrachter** am veröffentlichten Brett, und `claude.ai` ist im Browser-Fenster per
Richtlinie gesperrt — Claude kann diesen Weg nicht selbst fahren. Der Ablauf steht in der
Test-Konvention der `CLAUDE.md` (Weg 3c). Bis er gelaufen ist, sind R2/R3/R4 **am Code
belegt, nicht am Verhalten**.

**Bewusst nicht gebaut** (Feature-Kandidaten, keine Grundlage):
Warnung ab 800 Elementen (O3 bleibt dokumentiert) · „Unsortiert aufräumen" · Rückgängig
für das Reiter-Löschen (in E1 als optional benannt) · alles aus den offenen Punkten.

## Nachträge aus der Plan-Brille 2026-09-07 (Gate-Runde `/projekt-fertig`)
Vier Brillen (klarheit · konsistenz · machbarkeit · **research**) über `PLAN.md` +
`CLAUDE.md` + `REGELN.md`, mit dem Ist-Stand von `code/` als Ausgangslage (kein Code-Review).
**Ergebnis: 0 🔴 · 9 🟠 · 8 🟡** — Gate nicht blockiert. Die 🟠 sind hier abgearbeitet.

**Bestätigt gegen die echte Quelle** (nicht gegen meine Beschreibung davon):
- **E2 trägt.** Der Plattform-Vertrag (`db.d.ts`) sagt wörtlich, dass gelieferte Snapshots
  eingefroren sind und ein unverändertes Dokument über Lieferungen hinweg *dasselbe* Objekt
  bleibt. Die Begründung „darf aufbewahrt werden" war in der Planungsrunde eine Behauptung —
  jetzt ist sie belegt.
- **Der Lease-Verzicht bei R1 war technisch richtig**, nicht nur Geschmack: der Vertrag nennt
  `acquire()` ausdrücklich *keine* Sicherheitsgrenze. → in E1 eingearbeitet.
- **Kein Eigenbau-Fehler.** Excalidraw und tldraw sind die etablierten freien Whiteboard-
  Bibliotheken, setzen aber beide React + Build voraus; eine offizielle Ein-Datei-Verteilung
  gibt es bei keinem. Unter der Invariante „eine Datei, kein Build" existiert die reife
  Lösung also nicht — hier wird nichts neu erfunden, das es fertig gäbe.
- **Auf CRDT/OT zu verzichten ist keine Ignoranz.** Die reife Lösung braucht eigene
  Server-Logik, die diese Plattform nicht hergibt. Dokument-weites last-writer-wins ist die
  anerkannte einfachere Variante derselben Problemklasse.

### 🟠 Abgearbeitet in diesem Zug
- [x] **N1 — Rücksetz-Wächter in `applySheets` hat eine Lücke (Z. 1597).**
  `if (sheets.size && !sheets.has(activeSheet))` — bei **null** Reitern greift der Zweig
  nicht, `activeSheet` bleibt auf einer gelöschten Kennung stehen, `refilter()` findet
  nichts, und das Brett sieht **leer** aus, obwohl alles da ist. Genau das R1-Szenario, nur
  nicht mehr zerstörend. **Selbst am Code nachgeprüft.**
  → Beim Bauen: das `sheets.size &&` streicht man. `sheetList()` liefert immer mindestens
  einen Reiter, die Und-Verknüpfung ist die ganze Lücke.
- [x] **N2 — R2 hätte den falschen Wächter kopiert.** Der Plan verlangte für `applySheets`
  „denselben Wächter wie `applyNodes`" — das ist `if (editing || drag)`. Ein laufendes
  **Reiter-Umbenennen** hängt aber an einer **eigenen** Variablen `tabEditing` (Z. 484,
  605-644). Wörtlich abgeschrieben hätte der Fix genau den Fall nicht geschützt, um den es
  geht. → Der Wächter für `applySheets` muss `editing || drag || tabEditing` prüfen; der
  gemeinsame Abarbeitungs-Pfad aus E2 wird entsprechend auch am Ende des Umbenennens
  (`done()` in `renameSheet`, Z. 612-624) gerufen.
- [x] **N3 — „an zwei Stellen" war falsch gezählt.** Acht Stellen, und sie brauchen
  unterschiedliche Behandlung. → vollständig in E1 aufgelöst (Liste je Stelle mit „Pflicht /
  bleibt / ändert sich").
- [x] **N4 — O4 bekommt eine Handlungsanweisung, und zwar eine bessere als ein Wächter.**
  Statt einen Drift-Wächter für das zweite Feldschema des Übergabe-Knopfes zu bauen, nutzt
  der Knopf künftig **dieselben Feldnamen wie die Datenbank** (`kind`/`color`/`z`/`sheet`
  statt `art`/`farbe`/`ebene`/`reiter`). Damit gibt es kein zweites Format mehr, das driften
  kann — die Fehlerklasse verschwindet statt bewacht zu werden. Der Reiter-Wert wird dabei
  über die Waisen-Regel aufgelöst (→ E1, Stelle „Übergabe-Export").
- [x] **N5 — Prüfweg für zwei Betrachter fehlte komplett.** R1 und R4 sind die zwei
  kritischsten Befunde und waren mit der bestehenden Test-Konvention (nur Ein-Betrachter-
  Wege) **nicht überprüfbar**. → Als **siebter Weg** in die Test-Konvention: dasselbe
  veröffentlichte Brett in **zwei Browser-Fenstern** öffnen, dann je Befund ein Ablauf:
  *R4* — in Fenster A ein Element beschriften und im Feld bleiben, in Fenster B ein anderes
  Element verschieben; A abschließen → die Änderung aus B muss **sofort** erscheinen.
  *R2* — in A tippen, in B einen Reiter umbenennen; A darf den Cursor **nicht** verlieren.
  *R1* — bei zwei Reitern in A Reiter 1 und in B Reiter 2 löschen; danach müssen **alle**
  Elemente auf „Unsortiert" stehen und das Brett darf **nicht** leer aussehen (das prüft N1
  mit). Eintragen in `CLAUDE.md` gehört in `/projekt-umsetzen` (Verfassung).
- [x] **N6 — E2 beantwortet den Kernfall nicht: aufbewahrter Stand betrifft das Element, das
  gerade fertig bearbeitet wurde.** → Festgelegt: **der lokale Abschluss schreibt zuerst,
  dann wird der aufbewahrte Stand angewendet.** Beispiel: Element `n1` trägt „Angebot".
  A tippt „Angebot senden" (noch nicht abgeschlossen). B schreibt „Angebot prüfen" → der
  Stand trifft ein und wird aufbewahrt. A schließt ab → `putNode` schreibt „Angebot senden"
  (A gewinnt in der Datenbank, last-writer-wins), **danach** wird der aufbewahrte Stand
  angewendet und zeigt A kurz „Angebot prüfen" — bis der eigene Schreibvorgang als neuer
  Stand zurückkommt und wieder „Angebot senden" zeigt. Ergebnis ist konsistent, aber es
  **flackert** einmal. Bewusst akzeptiert: der Datenstand ist korrekt, und die Alternative
  (den aufbewahrten Stand für das eigene Element überspringen) würde einen Sonderfall
  einführen, der bei mehreren Feldern falsch wird. Gehört als Satz ins Akzeptanzkriterium
  von R4, damit das Flackern beim Prüfen nicht als Fehler gilt.
- [x] **N7 — Undo ist mit last-writer-wins ein bekanntes, ungelöstes Problem.** Ein naives
  Mehrbenutzer-Undo rollt fremde, zwischenzeitlich eingetroffene Änderungen kommentarlos
  zurück; korrekt ist es nur mit Operationshistorie, die dieses Projekt bewusst nicht hat.
  → Bei den offenen Punkten notiert: **falls** Undo kommt, gehört „nur die eigene letzte
  Aktion, und nur wenn danach niemand geschrieben hat, sonst ablehnen" ins
  Akzeptanzkriterium. Sonst entsteht eine zweite stille Datenverlust-Klasse neben G8.
- [x] **N8 — Live-Zeiger gehören nicht in die Dokument-Datenbank.** Das etablierte Muster
  heißt Presence/Awareness und läuft über einen **ephemeren** Kanal, getrennt von den
  gespeicherten Daten. → bei den offenen Punkten notiert; die `room`-Fähigkeit ist genau
  dieser Kanal.
- [x] **N9 — O1 und O2 standen im Plan als offen, waren aber erledigt.** Beide
  Textänderungen wurden am 2026-09-07 in Schritt 1 dieses Gates bewusst per Hand in
  `CLAUDE.md` nachgezogen (die präzisierte Schriften-Invariante und der sechste Testweg mit
  Ergebnis). Wer nur `PLAN.md` liest, hätte eine falsche To-do-Liste bekommen. **Eigene
  Drift**, hier korrigiert.

### 🟡 Mitgenommen
- [x] **N10** Die Zahl „200 Elemente" in E1 war erfunden. Der Vertrag nennt für
  `resource_exhausted` keinen Schwellwert. → gestrichen, Begründung trägt ohne sie.
- [x] **N11** „Der erste Reiter" ist wohldefiniert (nach `order`, bei Gleichstand
  lexikografisch nach Kennung) — stand nur im Code, jetzt in E1.
- [x] **N12** Reiter-Löschen gibt keine Dokumente mehr frei; 5.000-Dokumente-Grenze je
  Artifact. → als Preis in E1 benannt, „Unsortiert aufräumen" als Kandidat notiert.
- [x] **N13** O3-Schwelle beziffert: **ab 800** von 1000 geladenen Elementen warnen.
- [x] **N14** CDN aufgelöst (Content Delivery Network) bei erster Verwendung.
- [x] **N15** E2s Abarbeitungs-Stellen verortet: `finish()` (Z. 1084-1096), `done()` in
  `renameSheet` (Z. 612-624), Drag-Ende im `mouseup`-Behandler (Z. 1338).
- [x] **N16** G7-Streit entschieden (→ E1, Nebenwirkung).
- [x] **N17** Ein Tombstone (`sheets/<id>` mit `deleted:true` behalten) wäre die Alternative
  zu N1, bringt aber ewig wachsende Reiter-Dokumente und eine Schema-Erweiterung. Verworfen,
  kein klarer Gewinn.

## Panel-Befunde 2026-09-07 (aus `/projekt-verifizieren`, Fassung 1.0) — abzuarbeiten
Vier Brillen (konsistenz · klarheit · machbarkeit · robustheit) über `CLAUDE.md`, `PLAN.md`,
`code/`. Die roten Befunde sind **am Code nachgeprüft**, nicht nur übernommen — Zeilen
beziehen sich auf `code/pinit.html` in Fassung 1.0.

### 🔴 Heute erreichbar
- [x] **R1 — Reiter-Lösch-Wettlauf räumt das Brett leer.** **ERLEDIGT in 1.1 via E1.** `deleteSheet` (Z. 654-656) prüft
  "der letzte Reiter kann nicht weg" nur am **lokalen** `sheets`-Stand. Zwei Betrachter, zwei
  Reiter, jeder löscht seinen: beide Prüfungen sehen 2, beide Löschungen betreffen
  verschiedene Dokumente (kein last-writer-wins-Konflikt), danach ist das Brett samt aller
  Knoten und Pfeile leer. Kein Rückgängig. **Berührt eine Entscheidung:** frischer
  Server-Stand vor dem Löschen (`sheets`-Sammlung lesen) · oder Lease über `acquire()` ·
  oder Risiko bewusst annehmen und dokumentieren.
  **→ ENTSCHIEDEN (E1):** keine der drei. Löschen entfernt nur den Reiter-Eintrag, die
  Waisen-Regel bringt die Elemente auf den ersten Reiter. Wettlauf wird harmlos statt
  geschlossen; kein Element geht mehr verloren.
- [x] **R2 — `applySheets` hat den Nebenläufigkeits-Wächter nicht.** **ERLEDIGT in 1.1** (Wächter ist `warteGrund()`, deckt auch `tabEditing` — Verhalten noch vom Menschen zu prüfen, Weg 3c). Z. 1587: kein
  `if (editing || drag) { pending = true; return; }`, anders als `applyNodes` (Z. 1547) und
  `applyEdges` (Z. 1570). Benennt ein anderer Betrachter einen Reiter um, während hier
  jemand tippt, läuft `render()` → `renderNodes()` durch und baut das gerade editierte
  Element neu — Fokus und Cursor sind weg. Ein-Zeilen-Fix.
- [x] **R3 — `beginEdit` setzt `editing` im Fehlerfall nicht zurück.** **ERLEDIGT in 1.1** (Verhalten noch vom Menschen zu prüfen, Weg 3c). Z. 1074:
  `if (!editing || editing.id !== id || !t.isConnected) return;` — ohne `editing = null`.
  Das Gegenstück in `renameSheet` (Z. 636) macht es richtig. Ist das Textfeld beim Feuern
  des Zeitgebers schon aus dem DOM (Auslöser: R2), bleibt `editing` gesetzt, es wurden nie
  `blur`/`keydown` angehängt, und **kein Pfad ruft je `finish()`**. Folge: dieser Betrachter
  sieht **keine fremden Änderungen mehr**, stumm. Genau die Fehlerklasse, die für
  `tabEditing` am 2026-09-07 schon gefixt wurde — an der Schwesterstelle nicht nachgezogen.
  Ein-Zeilen-Fix.
- [x] **R4 — der Wächter verwirft den Snapshot, statt ihn zu puffern.** **ERLEDIGT in 1.1 via E2** (Verhalten noch vom Menschen zu prüfen, Weg 3c). `applyNodes` und
  `applyEdges` setzen `pending = true` und kehren **vor** dem Aufbau von `next` zurück; der
  Inhalt ist damit weg. Beim Abarbeiten ruft `pull()` (Z. 1544) nur `render()` — es liest
  nichts nach. Der verpasste Stand kommt also nie an, sondern erst zufällig mit der nächsten
  fremden Schreibaktion. `pending` ist in dieser Form funktionslos. **Berührt eine
  Entscheidung:** Snapshot zwischenspeichern und beim Abarbeiten anwenden · oder beim
  Abarbeiten aktiv `get()` nachlesen. *(Eigener Nachprüf-Befund, nicht aus den Brillen.)*
  **→ ENTSCHIEDEN (E2):** Snapshot aufbewahren und beim Fallen des Wächters anwenden, kein
  Nachlesen. Ein gemeinsamer Abarbeitungs-Pfad; Baureihenfolge R3 → R4 → R2.

### 🟠 Abweichungen
- [x] **O1 — Schriften vom CDN (Content Delivery Network) widersprechen der eigenen Invariante.** **ERLEDIGT 2026-09-07** (per Hand in Schritt 1 des Gates, → N9). Z. 2-3 laden
  `fonts.googleapis.com` / `fonts.gstatic.com`; `CLAUDE.md` erklärt "keine Bibliothek vom
  CDN, die Seite muss aus sich heraus laufen". Die Seite bricht ohne Netz nicht (die
  Rückfall-Schriftenliste greift), sieht aber anders aus als getestet — in gesperrten
  Umgebungen relevant. Entweder Invariante präzisieren (Web-Schriften als bewusste
  Ausnahme) oder Schriften einbetten. Von **zwei** Brillen unabhängig gemeldet.
  **→ ENTSCHIEDEN (E3):** Invariante präzisieren, Schriften bleiben. Textänderung erst in
  `/projekt-umsetzen` — dieser Skill darf die Verfassung nicht anfassen.
  **NACHTRAG: bereits erledigt.** Der Wortlaut steht seit dem 2026-09-07 in `CLAUDE.md`,
  bewusst per Hand in Schritt 1 des Gates nachgezogen (→ N9).
- [x] **O2 — der Vorlage-Pfad ist nicht belegt.** **ERLEDIGT** — am 2026-09-07 vom Nutzer am echten Artifact gesichtet, und der sechste Weg steht in der Test-Konvention. Die Kernkonstruktion hängt daran, dass
  `claude.use("db")` ohne `db`-Deklaration `null` liefert (Z. 1630). Die Test-Konvention
  listet fünf geprüfte Wege — dieser ist keiner davon; lokal greift ein **anderer** Zweig
  (der `catch`, weil `claude` dort gar nicht existiert). Nach der Regel "Werkzeug-Semantik
  wird gemessen, nicht geglaubt" fehlt der Probelauf am echten Vorlage-Artifact.
  → als sechsten Weg in die Test-Konvention und einmal ausführen.
  **→ TEILWEISE ERLEDIGT:** am 2026-09-07 vom Nutzer am echten Artifact gesichtet, beides
  vorhanden — die Konstruktion ist belegt. Offen bleibt nur das Eintragen in die
  Test-Konvention. **NACHTRAG: auch das ist erledigt** — der sechste Weg steht seit dem
  2026-09-07 in `CLAUDE.md`, samt Ergebnis. Offen bleibt nur noch der **siebte** Weg für
  zwei Betrachter (→ N5).
- [x] **O3 — `limit(1000)` ist eine brettweite Kappung und steht nicht im Datenschema.** **TEILWEISE ERLEDIGT in 1.1:** die Grenze steht jetzt im Datenschema. Die Warnung ab 800 bleibt Feature-Kandidat.
  Z. 1644 und 1652 begrenzen `nodes` und `edges` über **alle** Reiter hinweg, obwohl pro
  Reiter gefiltert wird. Jenseits von 1000 Elementen verschwinden Elemente aus der Ansicht
  ohne Fehler oder Hinweis. → Grenze dokumentieren, **Warnung ab 800** geladenen Elementen
  (→ N13) als Feature-Kandidat.
- [x] **O4 — der Übergabe-Knopf hat ein zweites, unbewachtes Datenformat.** **ERLEDIGT in 1.1 via N4** — gleiche Feldnamen wie die Datenbank, kein zweites Format mehr. Z. 1483-1518
  schreibt `titel`/`reiter`/`elemente`/`pfeile` mit den Feldnamen `art`/`farbe`/`ebene`
  statt `kind`/`color`/`z`/`sheet`. Eine Schema-Änderung muss diesen Text mitziehen, und
  dafür gibt es — anders als für die Vorlage-Kopie — **keinen Drift-Wächter**.
- [x] **O5 — Fehler bei Reitern und Titel werden stumm geschluckt.** **ERLEDIGT in 1.1.** Z. 1634 und 1642 haben
  leere Fehler-Rückrufe; `nodes` und `edges` (Z. 1650/1654) melden `Speicher-Fehler: <code>`.
  Bei `quota_exceeded` oder `revoked` zeigt die Statuszeile weiter "gespeichert", während
  Reiter nicht mehr synchronisieren. → dieselbe Behandlung wie bei nodes/edges.
- [x] **O6 — Übergabe-Knopf und Bedienung fehlen in der Doku.** **ERLEDIGT in 1.1** — Landkarte vollständig, Abschnitt „Bedienung heute“ in `CLAUDE.md`. Die Code-Landkarte nennt
  `btn-handoff` nicht, und es gibt keinen Abschnitt "Bedienung heute" (Werkzeugwahl,
  Einzelauswahl, Tastenkürzel — "Pfeil: A" steht nur als Hinweistext im Code, Z. 1457).
  Widerspricht dem Anspruch "ohne Code-Lektüre planbar", scharf beim nächsten
  `/feature-neu`.

### 🟡 Kleinigkeiten (dürfen dokumentiert durchgehen)
- [x] **G1** *(erledigt in 1.1 — Landkarte vollständig, 13 Abschnitte in Dateireihenfolge)* Code-Landkarte lässt über die Hälfte der Datei aus (Ansicht/Zoom, Bearbeiten,
  Werkzeuge, Export) und nennt zwei Funktionsreihenfolgen falsch (`track` steht **vor**
  `putNode`, `verbinden` **nach** `applySheets`). → ergänzen oder "Auszug" schreiben.
- [x] **G2** *(erledigt in 1.1)* `capabilities`, `db`-Capability und MCP sind bei erster Verwendung nicht
  aufgelöst (`CLAUDE.md` Z. 12, 14, 17).
- [x] **G3** *(erledigt in 1.1 — Kommando steht in der Test-Konvention)* Testschritt "Skript aus der HTML herausziehen" nennt kein Kommando — zwei
  Ausführende extrahieren unterschiedlich. → konkreten Befehl hinschreiben.
- [x] **G4** *(erledigt in 1.1 — Abschnitt „Wie ein Empfänger sein Brett aktualisiert“ in `CLAUDE.md`)* Kein Ablauf beschrieben, was ein **Empfänger** bei einer neuen Fassung tut.
- [x] **G5** *(erledigt in 1.1)* Das "Wie" des Republish mit `url` fehlt (nur das Warum steht da).
- [x] **G6** *(erledigt in 1.1 — 2000/200/80 Zeichen)* Kein Längen-Clamp für Text aus der Datenbank (Z. 1557/1577) — das UI-Feld hat
  `maxlength="80"`, eingehende Fremddaten nicht. Latent, braucht einen fremden Schreiber.
- [x] **G7** *(entfällt in 1.1 — E1 lässt `deleteSheet` gar nichts mehr löschen)* Waisen-Pfeile möglich, wenn ein fremder Schreiber `sheet` von Pfeil und Knoten
  auseinanderlaufen lässt (Z. 663-668). Kein Absturz (Z. 943 prüft defensiv), aber
  Datenreste. Latent.
- [x] **G8 — BEWUSST AKZEPTIERT** (nicht behoben). Ohne Versionsfeld je Element ist es nicht behebbar, und ein Versionsfeld hieße Schema-Erweiterung plus Konflikt-Oberfläche für einen Fall, der bei einem Prozess-Brett selten ist. In der Test-Konvention (Weg 3c) steht das erwartete Flackern, damit es beim Prüfen nicht als Fehler zählt. Gleichzeitiges Editieren desselben Elements: wer zuletzt das Feld verlässt,
  überschreibt kommentarlos. Direkte Folge von last-writer-wins; ohne Versionsfeld nicht
  behebbar. Als bekanntes Risiko notiert.
- [x] **G9** *(erledigt in 1.1 — die Ladegrenzen stehen im Datenschema; eine gemessene Zeichen-Schwelle gibt es weiter nicht und ist ohne Messung auch nicht seriös anzugeben)* Ab welcher Elementzahl pro Reiter das Neuzeichnen spürbar wird, steht nirgends.
- [x] **G10** *(erledigt in 1.1 — bei den offenen Punkten erklärt)* "room-Fähigkeit" in den offenen Punkten ist nicht erklärt.
- [x] **G11 — BELEGT am 2026-09-07.** `write_db` wurde in diesem Lauf real benutzt (Batch-Schreiben in `sheets` und `nodes`, Einzel-`delete`, `read_db get` zur Gegenprobe) — die Fähigkeit existiert und bildet auf `nodes`/`edges`/`sheets`/`meta` ab. Seit N4 trägt die Nutzlast ohnehin die echten Feldnamen, die Abbildung ist damit die Identität. Der Übergabe-Text setzt voraus, dass ein `write_db` existiert und das JSON 1:1
  abbildet — nie real durchgespielt. Scheitert nur der Übergabeweg, kein Datenverlust.

### Nicht beanstandet (geprüft, in Ordnung)
Datenschema deckt sich mit dem Code · keine toten Verweise auf die alten Dateinamen ·
`vorlage-erzeugen.js` hält, was die Doku behauptet · keine Browser-Dialoge, kein
`requestAnimationFrame`, keine Fremdbibliothek im Code · alle `localStorage`-Zugriffe
abgesichert, Rückfall auf `fit()` sauber · keine Markup-Einschleusung (alles über
`textContent`) · unbekannte `kind`-Werte und fehlende Felder werden defensiv behandelt ·
`z`-Drift bei "nach vorn/hinten" praktisch unerreichbar · Inhalt liegt wirklich nur in der
Datenbank, ein Seiten-Update ist damit gefahrlos.

## Panel-Befunde Fassung 1.1 (Code-Panel 2026-09-07) — abzuarbeiten
Vier Brillen über `code/`, `CLAUDE.md`, `PLAN.md`. **1 🔴 · 7 🟠 · 8 🟡.** Keine einzige
**Entscheidung** ist falsch — es sind Tippfehler-Klasse, ein vergessener Wächter-Aufruf und
Doku. Darum die Kante `fertig → in Arbeit` statt eines Rücksprungs auf `planung`
(Nutzer-Entscheidung: „umsetzen und dann verifizieren").

### 🔴 Heute erreichbar
- [x] **P1 — ERLEDIGT in 1.2, belegt.** Knopf geklickt: kein Wurf, 1753 Zeichen in der Ablage, Toast da, echte Feldnamen drin, alte weg. Testweg verschärft (Schritt 5 verlangt jetzt den Klick). Ursprünglich: — Der Übergabe-Knopf der Vorlage wirft und tut nichts.** `pinit.html:1576`
  liest `out.elemente.length`; `out` wird in Z. 1533 als `{meta, sheets, nodes, edges}`
  gebaut. `out.elemente` ist `undefined`, `.length` darauf wirft **vor** `copyOut(...)` —
  nichts landet in der Ablage, kein Hinweis, der Knopf tut sichtbar gar nichts. Das ist der
  **einzige interaktive Zweck** der org-weit geteilten Vorlage. Rest des alten Feldschemas,
  das N4 abgelöst hat. Von **zwei** Brillen unabhängig gemeldet, **selbst am Code
  nachgeprüft**.
  → Fix: `Object.keys(out.nodes).length`.
  → **Der eigentliche Befund ist die Testlücke:** Weg 3b/6 prüft nur, ob der Knopf
  *sichtbar* ist, nicht ob er *tut*. Genau deshalb ist es durchgerutscht. Der Weg muss den
  Klick verlangen.

### 🟠 Abweichungen
- [x] **P2 — ERLEDIGT in 1.2.** Prüfung als `pruefeAktivenReiter()` herausgezogen und in allen drei `apply*` gerufen; dazu ein `geladen`-Merker, damit der Ladewettlauf nicht als „keine Waisen“ zählt. Ursprünglich: — Der Gültigkeits-Wächter für den offenen Reiter sitzt nur in `applySheets`.**
  `applyNodes` (Z. 1623-1644) und `applyEdges` (Z. 1646-1662) machen nur
  `refilter(); render();`. Aber `waisenZahl()` hängt an **allNodes/allEdges** — die
  Entscheidung „ist `Unsortiert` noch anzeigbar" wird also an den falschen Stellen nicht
  getroffen. Zwei Abläufe:
  · **Einzel-Betrachter, Ladewettlauf (der schärfere Fall):** `rb.sheet` steht auf
    `_unsortiert`. Kommt der `sheets`-Stand **vor** den Elementen, ist `allNodes` noch leer,
    `waisenZahl()` ist 0, `gueltig` wird fälschlich falsch → es springt weg **und
    überschreibt den Merker** in `localStorage` (Z. 1683-1684). Nicht rückgängig.
  · **Zwei Betrachter:** A steht auf `Unsortiert`, B löscht die letzte Waise → bei A leeres
    Brett, kein Reiter als aktiv markiert.
  → Fix: die Prüfung als eigene Funktion herausziehen und am Ende **aller drei** `apply*`
  rufen; zusätzlich einen „schon geladen"-Merker, damit der Ladewettlauf nicht als
  „keine Waisen" zählt.
- [x] **P3 — ERLEDIGT in 1.2, belegt.** Auf dem Auffang-Reiter kein Anlege-Zeiger, Anlegen abgelehnt mit Hinweis, Werkzeug springt auf Auswählen; auf echten Reitern unverändert. Ursprünglich: — Elemente, die auf dem Auffang-Reiter angelegt werden, bleiben dort
  festgenagelt.** `addNode` (Z. 1083-1097) schreibt `sheet: activeSheet` ohne Sonderfall;
  steht der Betrachter auf `Unsortiert`, entsteht `sheet: "_unsortiert"`. `applySheets`
  verwirft jedes eingehende Dokument mit dieser Kennung (Z. 1668), also kann es nie einen
  echten Reiter dieses Namens geben — anders als bei einer echten Waise holt kein neuer
  Reiter das Element zurück.
  **Korrektur an der Brille:** sie schreibt „nie wieder erreichbar". Das ist **nicht**
  bestätigt — das Element ist auf `Unsortiert` sichtbar, auswählbar und bearbeitbar. Es ist
  *festgenagelt*, nicht verloren. Selbst gegengeprüft (`refilter` nimmt es auf, wenn
  `activeSheet === CATCH`).
  → Fix: auf dem Auffang-Reiter nichts anlegen lassen (Anlege-Werkzeuge dort inaktiv, mit
  Hinweis) — er ist ein Ablage-, kein Arbeitsort.
- [x] **P4 — `PLAN.md` ist zu über der Hälfte Archiv und wird bei jeder Session komplett
  geladen.** Rund 270 von 500 Zeilen waren abgehakte Historie; die Root-`CLAUDE.md` verlangt
  „schlank halten". **ERLEDIGT** — nach `artefakte/HISTORIE-2026-09-07.md` ausgelagert, hier
  bleiben Ziel, Architektur, Entscheidungen, offene Punkte, Changelog und die offenen
  Aufgaben.
- [x] **P5 — ERLEDIGT in 1.2.** Test-Konvention neu geschrieben: acht Schritte 1-8 in Leserichtung, Ausführungs-Nachweise als eigener Abschnitt. Ursprünglich: — Die Test-Konvention ist durcheinander numeriert.** `CLAUDE.md`: die Schritte
  laufen `1, 2, 3, 3b, 4, 5, 6, 3c` — der siebte Weg („3c", zwei Betrachter) steht **nach**
  Schritt 6. Wer linear abarbeitet, hält die Liste nach 6 für fertig und übersieht genau den
  Schritt, der die drei Nebenläufigkeits-Fixes abdeckt. Von **zwei** Brillen gemeldet.
  → Fix: durchgehend neu numerieren, die Ausführungs-Nachweise als eigener Abschnitt ohne
  fortlaufende Nummer.
- [x] **P6 — ERLEDIGT in 1.2** (Doku: Fenster nach Kennung, hält die ältesten). Ursprünglich: — Die Ladegrenze ist ein Fenster nach Dokument-Kennung, nicht ein zufälliges
  Fehlen.** Der Plattform-Vertrag: ohne `orderBy` sortiert die Abfrage nach Dokument-Kennung
  aufsteigend, und ein `limit` ist „ein Fenster" — Dokumente jenseits davon werden nicht
  geliefert, bis sie hineinrutschen. `uid()` baut die Kennung aus `Date.now()`, also
  zeitlich aufsteigend. Jenseits von 1000 Elementen hält das Fenster damit die **ältesten**;
  **neue Arbeit taucht dauerhaft nicht mehr auf**. Die Doku sagt harmlos „Elemente fehlen".
  → Fix: Richtung in der Doku benennen. (Ein Code-Fix wäre eine andere Kennungs-Strategie
  oder Paginierung — Feature-Kandidat, nicht jetzt.)
- [x] **P7 — ERLEDIGT in 1.2** (Schreiber-Hinweise: Reiter vor Elementen). Ursprünglich: — Der Schreiber-Hinweis nennt den wahrscheinlicheren Fall nicht.** `CLAUDE.md`
  sagt nur „Löschen schreibt `sheet` nicht um". Für einen Automaten (Claude über `write_db`,
  ein Import) ist der häufigere Fall: Element schreiben, **bevor** der Reiter existiert →
  es landet sofort auf `Unsortiert`, ohne Fehler.
  → Fix: ein Satz — Reiter vor den Elementen anlegen.
- [x] **P8 — ERLEDIGT in 1.2** (Doku: nicht anlegen, nur per `write_db delete` entfernbar). Ursprünglich: — Ein fremd angelegtes `sheets/_unsortiert` ist unlöschbar und frisst
  Kontingent.** `applySheets` überspringt es beim Einlesen (richtig), aber es zählt dauerhaft
  gegen die 5.000-Dokumente-Grenze und `deleteSheet` verweigert die Kennung. Latent (braucht
  einen gezielten Fremdschreiber).
  → Fix: in der Doku benennen; kein Code-Fix.

### 🟡 Kleinigkeiten
- [x] **P9** *(erledigt in 1.2)* Code-Landkarte, Abschnitt 3: `FASSUNG` steht im Code **vor** `KINDS`/`COLORS`,
  die Landkarte behauptet die umgekehrte Reihenfolge — und beansprucht „in Dateireihenfolge".
- [x] **P10** *(erledigt in 1.2, `btn-help` ergänzt)* Code-Landkarte, Abschnitt 12: die tatsächliche Reihenfolge ist Zoom → Löschen →
  Nach-vorn → Pfeiltext-Feld → Nach-hinten → Duplizieren → Titel → toast → **`btn-help`
  (fehlt ganz)** → copyOut → handoff → mermaid.
- [x] **P11** *(erledigt in 1.2)* „Bedienung heute" nennt nicht, dass **Doppelklick auf leere Fläche** einen
  Kasten anlegt (`canvas`-`dblclick`, Z. 1385-1398).
- [x] **P12** *(erledigt in 1.2 — `list`/`waisen`/`echt` einmal gerechnet)* `renderTabs()` rechnet mehrfach dasselbe: `sheetList()`/`echteReiter()` an vier
  Stellen plus ein `waisenZahl()`, und `echteReiter()` ruft `sheetList()` erneut — macht ~5
  Durchläufe über alle Elemente je Neuzeichnen. Kein Laufzeitproblem (beim Ziehen wird
  `render()` nicht gerufen), aber echte Redundanz. → `var list = sheetList()` einmal.
- [x] **P13** *(erledigt in 1.2)* `beginEdit`, Z. 1118: der Zweig, der `editing = null` setzt (R3-Fix), ruft
  **kein** `abarbeiten()` — anders als `finish()`. Erreichbarkeit unbestätigt (der Pfad
  scheint praktisch nicht auslösbar), Fix kostet nichts. → symmetrisch ergänzen.
- [x] **P14** *(erledigt in 1.2 — neuer Schritt 7 der Test-Konvention; steht beim nächsten Lauf zur Ausführung an)* Testlücke: kein Weg prüft eine **lokale** Aktion (Farbwahl, Duplizieren, nach
  vorn/hinten) **während** ein Textfeld offen ist. Alle Interleaving-Wege sind
  Cross-Betrachter.
- [x] **P15** *(erledigt in 1.2 — gestrichelt abgesetzt, kursiv, Punkt-Markierung)* Der Auffang-Reiter hat keine sichtbare Markierung — ein zweiter Betrachter
  sieht nur `Unsortiert (12)` und muss den Mouseover finden, um zu verstehen, was das ist.
- [x] **P16** *(erledigt in 1.2)* Kein Weg dokumentiert, **wie** ein Kollege überhaupt an `pinit.html`
  kommt (Anhang, Team-Laufwerk, Freigabe) — nur, was er danach tut.

### Von den Brillen bestätigt (kein Befund)
Das Aufbewahren der Snapshots ist **vertragskonform** (`db.d.ts`: gelieferte Snapshots sind
eingefroren, ein unverändertes Dokument bleibt dasselbe Objekt) · `docChanges()` wird
nirgends benutzt — richtig, es wäre beim späteren Anwenden falsch · `abarbeiten()` kann sich
nicht selbst aufrufen und wendet nichts doppelt an · die drei `render()`-Aufrufe darin malen
keinen Zwischenzustand, weil alles synchron in einer Aufgabe läuft · die Sortierung der
Reiter läuft zwischen Betrachtern nicht auseinander · `deleteSheet` kann nie den
Auffang-Reiter als ersten liefern · die 5.000-Dokumente-Grenze ist korrekt zitiert · der
lokale Guard „letzter Reiter kann nicht weg" ist **kein** Duplikat der Waisen-Regel (ohne ihn
kollabiert bei `sheets.size === 0` jede Gruppierung auf `haupt`) · die drei verbliebenen
`|| HOME`-Stellen sind genau die, die laut Plan bleiben sollten · alle Invarianten halten.

## Panel-Befunde Fassung 1.2 (Code-Panel 2026-09-07) — alle in 1.3 abgearbeitet
**1 🔴 · 7 🟠 · 7 🟡.** Nachgetragen: dieser Lauf ging ohne Befundliste im Plan direkt ins
Bauen, und der Status wurde nicht auf `in Arbeit` gesetzt — zwei Verfahrensabkürzungen, vom
Konkurrenz-Guard am Ende aufgedeckt. Hier die Liste, wie sie hätte stehen müssen.

### 🔴
- [x] **Q1 — Das Anlege-Verbot des Auffang-Reiters galt nur für einen von drei Wegen.**
  Der Guard saß im `mousedown`-Zweig `if (KINDS[tool])`. Ungeschützt blieben **Doppelklick
  auf leere Fläche** (`addNode("box", …)` direkt im `dblclick`-Handler — der Weg, den die
  Doku in derselben Runde als Schnellweg beschrieb) und das **Pfeil-Werkzeug** über
  `connect()`. Von **zwei** Brillen unabhängig gemeldet, selbst am Code nachgeprüft.
  Mein „ERLEDIGT in 1.2, belegt“ war falsch: der Test prüfte genau den Weg, der geschützt
  war. → **In 1.3:** Verbot als `catchGesperrt()` **in** `addNode()` und `connect()`, alle
  drei Wege belegt, Gegenprobe auf echtem Reiter gemacht.

### 🟠
- [x] **Q2 — `geladen` wurde beim ersten Snapshot gesetzt, nicht beim server-endgültigen.**
  Zwei Brillen, am Plattform-Vertrag belegt: die erste Lieferung einer Anmeldung ist oft
  `fromCache` und kann leer sein. Der Ladewettlauf war damit nur verschoben. Dieselbe Datei
  hatte den richtigen Kunstgriff schon (`firstNodes` prüft `!fromCache`) — für `geladen`
  fehlte er. → **In 1.3** nachgezogen und in der Verfassung dokumentiert.
- [x] **Q3 — Der Reiterwechsel durch fremdes Löschen zog Kamera und Hinweis nicht mit.**
  Anders als `switchSheet`/`deleteSheet` rief `pruefeAktivenReiter()` kein `restoreView()`
  und keinen Toast — der Betrachter sah ein scheinbar leeres Brett ohne Erklärung.
  → **In 1.3:** Hinweis + Kamera nachgeholt, und der Hinweis **schweigt beim ersten
  Abgleich** (dort hat niemand gelöscht — eigener Fund beim Durchlesen des Fixes).
- [x] **Q4 — Nach einem Lesefehler meldete sich kein Abonnement neu an**, und die
  Fehlermeldung wurde vom nächsten eigenen Schreibvorgang mit „gespeichert“ überschrieben —
  der Stillstand wurde unsichtbar. Der Vertrag nennt eine neue Anmeldung als einzigen
  Rückweg. → **In 1.3:** `syncTot`-Merker, Meldung „Verbindung verloren (…) — bitte Seite
  neu laden“ bleibt stehen. Bewusst **keine** automatische Wiederanmeldung (kann selbst
  scheitern und im Kreis laufen).
- [x] **Q5 — Die Befundliste P1-P16 war selbst schon das nächste Archiv** (110 von 357
  Zeilen, alles abgehakt) — dieselbe Klasse wie P4 eine Runde vorher. → **In 1.3**
  ausgelagert, plus eine Regel im Plan: eine Befundliste wandert **im selben Zug**, in dem
  der letzte Punkt abgehakt wird.
- [x] **Q6 — Die Ausführungs-Nachweise standen nicht chronologisch** (1.0, 1.2, 1.1) — 1.2
  war eingefügt statt angehängt. → **In 1.3** sortiert.
- [x] **Q7 — Widerspruch von mir gegen mich:** die Einleitung der Test-Konvention sagte
  „Schritte 5-7 brauchen das veröffentlichte Artifact“, mein eigener Nachweis sagte „Schritt
  7 ist ein lokaler Ablauf“. → **In 1.3** aufgelöst: lokal sind 1-4, 7, 8; veröffentlicht
  nur 5 und 6.
- [x] **Q8 — G2 war falsch abgehakt:** `MCP` und `capabilities` waren nirgends aufgelöst
  (selbst nachgeprüft: nichts). → **In 1.3** beides bei erster Verwendung erklärt.

### 🟡
- [x] **Q9** `waisenZahl()` lief zwei Durchläufe je Neuzeichnen, nicht einen — meine
  1.2-Notiz war ungenau. → `sheetList()` hinterlegt die Zahl in `letzteWaisen`, ein
  Durchlauf.
- [x] **Q10** Landkarte: Zustands-Merker nicht in Deklarationsreihenfolge. → nachgezogen,
  inklusive der neuen Merker.
- [x] **Q11** Anlege-Zeiger wurde nach einem stillen Reiterwechsel nicht neu bewertet.
  → `setTool(tool)` in `pruefeAktivenReiter()`.
- [x] **Q12** Der `geladen`-Merker war nur genannt, nicht erklärt — und seine einzige
  Erklärung stand in einer Befundliste, die als nächstes archiviert wurde. → eigener Absatz
  in der Verfassung.
- [x] **Q13** Fußnote in dieser Datei, wo E1-E3 stehen. → ergänzt.
- [x] **Q14** `btn-handoff` rief `echteReiter()` zweimal. → eine lokale Variable.
- [x] **Q15** Der 1.1-Nachweis nannte Schritt 8, den es damals noch nicht gab.
  **Korrektur an der Brille:** die Rot-Gegenprobe **lief** in der 1.1-Runde — nur die Notiz
  im Bau-Protokoll fehlte. Kein falscher Nachweis, ein fehlender Eintrag. → nachgetragen.

### Offen gelassen (bewusst, Wartungsliste)
- **Verwaiste Kante durch Wettlauf beim Pfeil-Ziehen:** löscht ein zweiter Betrachter
  während eines Pfeil-Ziehens den Quell-Knoten, entsteht eine Kante auf eine gelöschte
  Kennung — unsichtbar (`renderWires` überspringt Kanten ohne beide Enden), zählt aber gegen
  die 5.000-Dokumente-Grenze. Referenziell, nicht reiter-bezogen: die Waisen-Regel erfasst
  ihn nicht. Sehr enges Zwei-Betrachter-Fenster. Braucht eine Entscheidung (Aufräum-Lauf?
  Prüfung in `connect()` gegen den Serverstand?) — darum nicht in einer Fix-Runde erledigt.

## Panel-Befunde Fassung 1.3 (Code-Panel 2026-09-07) — in 1.4 abgearbeitet
**2 🔴 · 7 🟠 · 8 🟡.** Der erste 🔴 hat das **veröffentlichte Brett gebrochen** — Fassung 1.3
war live und unbrauchbar, bis 1.4 nachgeschoben wurde.

### 🔴
- [x] **S1 — `reiterAbgleichLief` wurde benutzt, aber nie deklariert.** Unter `"use strict"`
  ein `ReferenceError` bei **jedem** Snapshot: `pruefeAktivenReiter()` brach ab, `refilter()`
  lief nie, das Brett zeigte den Datenbank-Inhalt nicht mehr. Von **zwei** Brillen unabhängig
  gemeldet, selbst nachgeprüft (Gegenprobe: eine Zuweisung an einen nicht deklarierten Namen
  wirft unter strict).
  **Wie es passiert ist:** Die Deklaration und die Verwendung entstanden in **zwei**
  Patch-Läufen. Der erste Lauf enthielt beide, brach aber an einem falschen Anker ab — und
  mein Patch-Skript schrieb erst am Ende, also landete **nichts**. Der zweite Lauf setzte nur
  die Verwendung. Genau der Fehlermodus, den ich nach der 1.1-Runde als Feedback
  aufgeschrieben hatte. → **In 1.4:** Deklaration ergänzt, **und das Patch-Skript umgebaut**
  (erst alle Anker prüfen, dann schreiben, dann melden).
  **Warum es lokal nicht auffiel:** der Pfad läuft nur mit echter Datenbank. Lokal greift
  `vorlageModus()`. Die einzigen Schritte, die es gefangen hätten, sind 6 und 7 — und die
  stehen laut eigenem Protokoll aus. `node --check` sieht so etwas grundsätzlich nicht.
  → **In 1.4:** neuer Prüfer `code/undeklariert-pruefen.js` als **Schritt 3** der
  Test-Konvention, mit Rot-Gegenprobe belegt.
- [x] **S2 — „Duplizieren" war ein vierter Anlege-Weg, der den Guard umging.** Der
  `btn-dup`-Handler baut sein Element selbst und ruft weder `addNode()` noch `connect()`.
  Auf dem Auffang-Reiter entstanden so beliebig viele neue Dokumente — gegen die Invariante,
  die eine Runde vorher genau dafür geschrieben wurde. → **In 1.4:** `catchGesperrt()` im
  Handler, belegt (Auswahl steht, Knopf sichtbar, Guard greift; Gegenprobe auf echtem Reiter
  duplizierte weiter).
  **Die Lehre:** mein `grep` fragte „wer ruft `addNode`?" — die richtige Frage war „wer legt
  Dokumente an?" (`allNodes.set|allEdges.set`). Die Erzeuger-Liste steht jetzt als Kommentar
  am Guard.

### 🟠
- [x] **S3 — `track()` überschrieb die Verbindungs-Meldung.** `saved()`, `busy()` und
  `render()` respektierten `syncTot`, `track()`s eigener Fehlerpfad nicht — nach einem
  Verbindungsverlust ersetzte „Speichern fehlgeschlagen" die Ursache. Dieselbe
  Geschwister-Form wie S2. → gefixt.
- [x] **S4 — `revoked` bekam die falsche Meldung.** Der Vertrag sagt für diesen Code
  ausdrücklich: nicht erklären, warum — und Neuladen hilft nicht, der Zugriff bleibt
  entzogen. Ebenso bei `invalid_argument` (der Aufruf selbst ist falsch). → `syncGestorben`
  verzweigt jetzt: „kein Zugriff mehr", „Fehler in der Abfrage — neu laden hilft nicht",
  „Brett ist voll", sonst „Verbindung verloren".
- [x] **S5 — Die Invariante war zu eng.** „Ein Verbot gehört in die Funktion" deckt nur die
  Form „eine Funktion, viele Aufrufer". Die andere Form — **mehrere gleichrangige
  Geschwisterfunktionen** — hat viermal zugeschlagen (N2, P2, S2, S3) und hat kein
  Zentrum, in das man etwas legen könnte. Die Klarheits-Brille hat das erkannt **und damit
  S3 vorhergesagt**, bevor eine andere Brille ihn fand. → Lehre allgemeiner gefasst, beide
  Formen mit Beispielen, plus der Satz über die richtige Suchfrage.
- [x] **S6** Die drei neuen Merker (`letzteWaisen`, `reiterAbgleichLief`, `syncTot`) standen
  nur als Namen in der Landkarte. → je ein Satz.
- [x] **S7** `PLAN.md` nannte Q1-Q15 nicht in seinem Historie-Verweis. → ergänzt.
- [x] **S8** Landkarte: `syncTot` stand im falschen Abschnitt, `syncGestorben` fehlte ganz.
  → beide richtig eingeordnet.
- [ ] **S9 — `PLAN.md` ist wieder halb Archiv: E1-E3 (~145 von 257 Zeilen).** Die
  Entscheidungen sind vollständig gebaut und stehen als Invarianten in `CLAUDE.md`; die
  Herleitung samt Code-Zeilennummern und erledigten Regieanweisungen („das passiert in
  `/projekt-umsetzen`") liest sich für einen neuen Leser wie offene Arbeit. Meine
  Auslagerungs-Regel spricht wörtlich nur von **Befundlisten** und greift deshalb nicht.
  **OFFEN** — bewusst nicht in derselben Runde erledigt, in der zwei 🔴 zu fixen waren.

### 🟡 (nicht erledigt, Wartungsliste)
- [ ] **S10** Der Fehlercode in der Nutzer-Meldung („unavailable") ist eine unerklärte
  Fachabkürzung mitten im Satz. Teilweise durch S4 entschärft, aber der Rest-Fall nennt
  weiter `wo`.
- [ ] **S11** `waisenZahl()` läuft dreimal je Snapshot (zweimal `waisenZahl`, einmal
  `refilter`), nicht einmal — die Doku-Aussage zu `letzteWaisen` ist ungenau. Rechnerisch
  belanglos bei den Ladegrenzen.
- [ ] **S12** `firstNodes` und `geladen.nodes` werten dasselbe `!fromCache`-Signal doppelt
  aus; ein Merker würde reichen.
- [ ] **S13** Auslagerungs-Regel hat keinen Durchsetzungs-Mechanismus (Vorschlag der Brille:
  eine Zeilenzahl nennen, ab der geprüft wird).
- [ ] **S14** Historie-Datei ohne Kopf-Index (welche Kennung zu welchem Lauf gehört).
- [ ] **S15** `btn-handoff` in der Landkarte nicht als vorlage-exklusiv gekennzeichnet.
- [ ] **S16** Doppeltes `setTool("select")` nach abgelehntem `connect()` — idempotent, kein
  Zustands-Loch.
- [ ] **S17** Verwaiste Kante durch Wettlauf beim Pfeil-Ziehen (schon aus dem 1.2-Panel
  offen, referenziell statt reiter-bezogen, braucht eine Entscheidung).


---

# Nachtrag (ausgelagert aus PLAN.md am 2026-09-07, zweite Verschlankung)

## Entschieden 2026-09-07 (Planungsrunde nach dem Panel)
Diese Runde beantwortet die drei Panel-Befunde, die eine Entscheidung berührten (R1, R4, O1).
R2, R3 und die übrigen 🟠/🟡 sind mechanisch und brauchten keine.

### E1 — Ein Reiter zu löschen darf keinen Inhalt vernichten (löst R1)
**Fassung 2 (2026-09-07, nach der Plan-Brille — Research-Befund F1).** Die erste Fassung
schickte Waisen auf den *ersten* Reiter. Das war die schwächere Variante: die Reste sind
dort nicht von echtem Inhalt zu unterscheiden. Jetzt gilt ein **Auffang-Reiter**.

**Entscheidung:** Löschen entfernt **nur** das Dokument `sheets/<id>`. Die Elemente werden
**nicht angefasst** — ihr `sheet`-Feld bleibt stehen (also **keine** Umschreibung, die
Auflösung passiert erst beim Anzeigen). Dazu eine Regel im Datenmodell, die
**Waisen-Regel**:

> Der wirksame Reiter eines Elements ist der eigene, **wenn es diesen Reiter gibt** — sonst
> der **Auffang-Reiter `_unsortiert`** (Anzeigename „Unsortiert"). Gibt es überhaupt keinen
> benannten Reiter, gilt der implizite `haupt`.

**Der Auffang-Reiter ist virtuell.** Es gibt kein Dokument `sheets/_unsortiert`; er wird von
`sheetList()` **erzeugt**, und zwar nur dann, wenn mindestens ein Element auf ihn auflöst. Er
steht immer als **letzter** in der Reiterleiste, trägt die Anzahl (`Unsortiert (12)`) und
lässt sich **nicht umbenennen und nicht löschen**. Damit kollidiert er nicht mit den
Nutzer-Reitern und braucht keine Schema-Erweiterung.

**Warum ein Auffang-Reiter und nicht „der erste":** Das ist das etablierte Muster (Notion
legt verwaiste Kind-Seiten sichtbar an einem eigenen Ort ab, nicht in einer beliebigen
Nachbar-Seite). Es kostet **genau denselben einen Schreibvorgang** und behält die
Umkehrbarkeit — nur der Zielort ist erkennbar statt geraten.

**Warum nicht „Elemente verschieben":** Verschieben hätte je Element einen Schreibvorgang
gekostet. Der Plattform-Vertrag nennt für `resource_exhausted` unter anderem „too many
concurrent writes", **ohne Schwellwert** — ob ein konkreter Reiter das Budget reißt, ist
damit **ungemessen** und war in Fassung 1 mit einer erfundenen Zahl („200 Elemente")
begründet. Die Zahl ist gestrichen; die Entscheidung braucht sie nicht: **ein**
Schreibvorgang plus Umkehrbarkeit schlägt „viele Schreibvorgänge" ohnehin.

**Umkehrbarkeit:** Die Elemente wissen noch, wo sie hingehörten. Ein Reiter mit derselben
Kennung holt sie zurück. Ein „Rückgängig" direkt nach dem Löschen ist damit möglich
(Kennung + Name kurz im Speicher halten) — **optional**, nicht Teil der Fertig-Bedingung.
Dieses eng begrenzte Rückgängig ist vom allgemeinen Undo-Problem (→ N7) **nicht** betroffen:
es reaktiviert nur eine Kennung und schreibt kein fremdes Feld zurück.

**Was das mit R1 macht:** Der Lösch-Wettlauf wird **harmlos**, nicht zugemacht. Löschen zwei
Betrachter gleichzeitig beide Reiter, sind zwar beide Reiter weg — aber **kein Element**; sie
stehen dann auf „Unsortiert". Ein Lease (`acquire()`) wurde verworfen, und zwar **technisch
begründet**, nicht aus Geschmack: der Plattform-Vertrag sagt ausdrücklich, ein Lease ist
*keine* Sicherheitsgrenze — andere Betrachter können trotzdem direkt schreiben. Ein Lease
hätte den Wettlauf also nur geschlossen, wenn **jeder** Schreibpfad ihn respektiert, und
hätte den Fehlklick als Einbahnstraße stehengelassen. Der Fehlklick ist der wahrscheinlichere
Schaden.

**Betroffene Stellen im Code — hier steckt das eigentliche Umsetzungsrisiko.** Die Regel ist
kein Ein-Zeilen-Fix: der Fallback `sheet || HOME` kommt an **acht** Stellen vor (Z. 538, 539,
659, 664, 667, 780, 786, 1490, 1495 — gemessen, nicht geschätzt; die Zahl „zwei Stellen" aus
Fassung 1 war falsch). Sie sind **nicht** alle gleich zu behandeln:
- **`refilter()` (Z. 538-539) — Pflicht.** Filtert heute per Direktvergleich
  `(n.sheet || HOME) === activeSheet`. Wird nur der Löschpfad geändert und `refilter` nicht,
  landen Waisen auf **keinem** sichtbaren Reiter — die Daten überleben, sind aber nie
  erreichbar. Genau der Zustand, den E1 verhindern soll.
- **`sheetList()` (Z. 525-531) — Pflicht.** Muss den virtuellen `_unsortiert` erzeugen, wenn
  Waisen existieren, und ihn als letzten einsortieren. „Der erste" Reiter ist übrigens
  wohldefiniert: sortiert nach `order`, bei Gleichstand lexikografisch nach Kennung.
- **`deleteSheet()` (Z. 659, 664, 667) — ändert sich.** Löscht künftig keine Knoten/Pfeile
  mehr, nur `sheets/<id>`.
- **`putNode`/`putEdge` (Z. 780, 786) — bleiben.** Beim Schreiben gilt weiter das eigene
  `sheet`; die Auflösung ist reine Anzeige-Sache. **Nicht** umschreiben, sonst geht die
  Umkehrbarkeit verloren.
- **Übergabe-Export (Z. 1490, 1495) — Pflicht.** Schreibt heute `n.sheet || HOME` direkt aus
  `allNodes`, ungefiltert. Ein verwaister Verweis würde eine Reiter-Kennung ausgeben, die in
  `out.reiter` gar nicht vorkommt. Muss über die Waisen-Regel auflösen (→ zusammen mit N4).
- **Der Rücksetz-Wächter in `applySheets` (Z. 1597) hat eine eigene Lücke** — siehe N1.

**Nebenwirkung — G7 entfällt wirklich.** Die Konsistenz-Brille hatte bezweifelt, dass die
Waisen-Regel G7 (Waisen-Pfeile) mitlöst; die Machbarkeits-Brille hat die Ursache benannt und
damit den Streit entschieden: G7 entstand, weil `deleteSheet` Knoten **löschte**, auf die
anderswo Pfeile zeigten (Z. 663-668). E1 nimmt genau dieses Löschen weg — der Auslöser
entfällt, nicht nur der Symptomdruck.

**Preis, den E1 bewusst zahlt:** `deleteSheet` war der **einzige** automatische Rückbau —
Reiter löschen gab Dokumente frei. Das fällt weg, Elemente bleiben erhalten, bis sie einzeln
gelöscht werden. Die Datenbank eines Artifacts ist auf **5.000 Dokumente** gedeckelt; danach
schlägt jedes Anlegen mit `quota_exceeded` fehl (bestehende bleiben schreibbar). Für ein
persönliches Brett weit weg, aber ein „Unsortiert aufräumen"-Feature ist damit ein
Kandidat — steht bei den offenen Punkten.

**Achtung beim Umsetzen:** Das Datenschema in `CLAUDE.md` kennt heute nur die zwei alten
Fälle (Sammlung fehlt / Feld fehlt → `haupt`) und muss auf die Waisen-Regel nachgezogen
werden. `CLAUDE.md` ist Verfassung — das passiert in `/projekt-umsetzen` oder bewusst per
Hand, **nicht** in `/projekt-planen`.


### E2 — Verpasste Änderungen werden aufbewahrt, nicht verworfen (löst R4)
**Entscheidung:** Trifft ein Stand ein, während getippt oder gezogen wird, wird das
**Snapshot-Objekt aufbewahrt** (je Sammlung eines) und angewendet, sobald der Wächter fällt.
Kein Nachlesen über das Netz.

**Warum kein `get()` beim Abarbeiten:** Der aufbewahrte Stand ist per Definition der
neueste — ändert danach jemand etwas, kommt der nächste Stand von selbst. Ein `get()` würde
je Textedit eine Abfrage kosten und kann selbst scheitern, bräuchte also wieder eine
Fehlerbehandlung. Die Snapshot-Objekte sind laut Plattform-Vertrag unveränderlich und stabil,
dürfen also gehalten werden.

**Ein Abarbeitungs-Pfad für alles:** Der heutige `pending`/`pull()`-Mechanismus (`pull` ruft
nur `render()` und liest nichts nach) wird durch **eine** Funktion ersetzt, die alle
aufbewahrten Stände anwendet. Sie wird an **jeder** Stelle gerufen, an der der Wächter fällt:
Textedit fertig, Ziehen fertig, Reiter-Umbenennen fertig.

**Verzahnung mit R2 und R3 — die drei gehören in einen Zug:**
- R2 gibt `applySheets` denselben Wächter. Damit werden auch Reiter-Stände aufbewahrt.
- R3 verhindert, dass der Wächter **hängen** bleibt. Ohne R3 macht R2 die Lage schlimmer:
  ein klemmender `editing`-Merker friert dann auch die Reiter ein.
- Reihenfolge beim Bauen: **R3 zuerst** (Merker kann nicht klemmen), dann R4 (Aufbewahren),
  dann R2 (Wächter für Reiter).

### E3 — Web-Schriften sind die einzige erlaubte Netz-Abhängigkeit (löst O1)
**Entscheidung:** Die Invariante in `CLAUDE.md` wird **präzisiert**, die Schriften bleiben.
Der Wortlaut soll sagen: keine JS-/CSS-**Bibliothek** vom CDN, keine Laufzeit-Abfrage an einen
fremden Dienst — Web-Schriften sind die einzige Ausnahme, mit Begründung: sie sind kein Code,
es gibt eine Rückfall-Schriftenliste, und die Seite ist ohne Netz **voll funktionsfähig**, sie
sieht nur anders aus.

**Bewusst mitgenommen:** In gesperrten Umgebungen (Firewall, WDAC — im Haus real) sieht die
Seite anders aus als getestet. Das ist kosmetisch und akzeptiert. Wer es exakt gleich braucht,
kann die Schriften später einbetten; das ist dann eine Größen- gegen Treue-Abwägung, keine
Fehlerbehebung.

**Achtung beim Umsetzen:** Die Invarianten in `CLAUDE.md` sind Verfassung — `/projekt-planen`
darf sie nicht anfassen. Die Textänderung passiert in `/projekt-umsetzen` (oder bewusst per
Hand), nicht hier.

### Gemessen — Ergebnis liegt vor
- [x] **O2 gegengeprüft:** Zeigt die veröffentlichte Vorlage
  (`https://claude.ai/code/artifact/b059cf88-e25b-4460-a703-6a8abf4dd1f5`) wirklich das
  Abzeichen **VORLAGE** und die Statuszeile „Vorlage – nichts wird gespeichert"? Ich komme
  an `claude.ai` vom Browser-Fenster aus **nicht** heran (Richtlinie blockiert die Domain),
  kann diesen Weg also nicht selbst belegen.
  **Ergebnis 2026-09-07, Sichtprüfung durch den Nutzer: beides ist da.** Abzeichen
  VORLAGE und Statuszeile „Vorlage – nichts wird gespeichert“ erscheinen am echten
  Artifact. Damit ist belegt, dass `claude.use("db")` ohne `db`-Deklaration `null`
  liefert und `verbinden(null)` in `vorlageModus()` landet — die Kernkonstruktion trägt.
  **Rest von O2 bleibt offen:** diesen Weg als **sechsten** in die Test-Konvention der
  `CLAUDE.md` aufnehmen, damit er bei künftigen Fassungen nicht wieder ungeprüft bleibt.
  Gehört in `/projekt-umsetzen` — die Verfassung ist von hier aus nicht anfassbar.

## Offen nach Fassung 1.6
- **Das Code-Panel hat 1.5 und 1.6 noch nicht gesehen** (`/projekt-verifizieren`). Gebaut und
  lokal belegt sind Schritte 1-5, 8 und 9 der Test-Konvention (1.6: 5 nur teilweise).
- **Die beiden globalen Skills sind ungetestet am echten Brett.** `pinit-lesen` und
  `pinit-schreiben` liegen in `~/.claude/skills/` (bewusst nicht im Projekt — die Bretter
  gehören zu verschiedenen Projekten). Erster echter Lauf mit einer Brett-URL steht aus.
- **Verworfen:** ein Knopf „Als Markdown kopieren“ in der Seite — Claude liest die
  Datenbank direkt, ein zweiter Exportweg in der Seite wäre nur Pflege.
- **Schritte 6 und 7 kann nur der Mensch fahren** (veröffentlichtes Artifact, zwei
  Betrachter; `claude.ai` ist Claude im Browser-Fenster per Richtlinie gesperrt). Sie sind
  die einzigen Schritte, die die Fassung-1.3-Panne vor dem Veröffentlichen gefangen hätten.
- **Die neun Wartungspunkte S9-S17** aus dem 1.3-Panel stehen weiter offen — Liste in
  [`artefakte/HISTORIE-2026-09-07.md`](artefakte/HISTORIE-2026-09-07.md).

## Panel-Befunde — alle abgearbeitet
Die Befundlisten der Panel-Läufe stehen vollständig in
[`artefakte/HISTORIE-2026-09-07.md`](artefakte/HISTORIE-2026-09-07.md): R1-R4 / O1-O6 /
G1-G11 (Fassung 1.0), N1-N17 (Plan-Brille), P1-P16 (Fassung 1.1) und Q1-Q15 (Fassung 1.2).

**Regel für den nächsten Lauf:** eine Panel-Befundliste wird **im selben Zug** ausgelagert,
in dem der letzte Punkt abgehakt wird — nicht erst, wenn das nächste Panel sie als Archiv
meldet. Genau das ist hier zweimal passiert (P4, dann wieder im Panel zu 1.2).
